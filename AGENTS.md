# 小u的 UI 库使用规则

## 检索与选择

1. 先阅读 README.md、catalog.json 和当前项目的规则；只选择实际存在的条目。
2. 根据页面类型、主题、风格、信息密度、技术栈、响应式及授权情况筛选。
3. 用户喜欢/不喜欢的反馈优先于代理评分；不要擅自把未评价条目标成用户喜欢。
4. 组合组件时统一字体、颜色和交互，保留真实业务逻辑、数据和关键文案。

## 设计偏好

- 页面类型分为营销、工具、内容、展示；围绕主要任务建立层级。
- 禁止默认“左边大字、右边大图”首屏和机械套用 SaaS 三列卡片布局。
- 不使用深绿色背景、默认全局 Inter、大面积蓝紫渐变、装饰光球和泛滥毛玻璃。
- 英文标题优先 Space Grotesk；中文标题使用确实支持中文的字体，正文保证阅读舒适。
- 不把所有区块做成卡片，不堆卡片嵌套，不全局使用 transition-all。
- 动效有明确用途并支持 prefers-reduced-motion；熟悉操作优先使用现有图标库。
- 保证移动端、键盘焦点、触控尺寸和颜色对比度；不伪造业务事实。

## 收录流程

1. 检查用户给定网站的域名、跳转和风险；不能验证时明确记录未知。
2. 分析值得复用的具体部分；避免无选择地镜像整个网站。
3. 确认源码出处和许可。用户于 2026-09-12 明确要求只分析、拆解和保存原始源码，不擅自修改或重写。没有公开源码时标记缺失，等待用户提供授权源码，不根据截图自行重写。
4. 每条目保存 README.md、meta.json、src/，有运行预览时加入 previews/。
5. 记录依赖、运行命令、外部服务、授权、适用及不适用场景；验证桌面和移动端表现、主要交互。
6. 只有验证完成的条目才能标为 ready。没有源码的参考不能标为可复用组件。
7. 更新 catalog.json；保留第三方许可证和来源，不上传凭证或无关文件。
8. 需要 GitHub 同步时先检查当前差异和远程身份；只提交本次资料库文件，不强推、不覆盖未知改动。
9. vendor/ 内上游源码逐字节保留，不能格式化、改颜色、改文案、改默认参数或改依赖版本。分析、问题记录、索引写在 analysis/ 和 effects/。接入实际项目时若需要改动，先说明具体差异并获得用户指示。

## 验收

遵循当前任务适用的设计技能。网页实现必须运行后检查约 1440px 与 390px 截图及交互；修正后最多再确认一轮。不把未运行的代码描述为已验证，不把本地提交描述为已上传。
